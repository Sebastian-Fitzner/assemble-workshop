Kopiere die Übungsdateien in den Arbeitsordner. Öffne nun die Page `page-debugging.hbs`, die JSON-Datei `data/page-debugging/debug-teaser.json` und den Partial `debug-teaser.hbs`. 

Schau Dir danach den Output an. Dort fehlt leider die Klasse im Teaser, die in der Page definiert ist. 

Überprüfe deshalb, welchen Datenkontext der Partial bekommt. Probiere nun anhand dieser Info, den Fehler im Output zu beheben (_denke daran, wie man die Datenebene wechseln kann_). 

